services:
  api:
    build: ./backend
    ports:
      - "8080:8080"
    env_file:
      - .env.local
    environment:
      - API_PORT=8080
      - OLLAMA_URL=http://host.docker.internal:11434
      - OLLAMA_MODEL=llama3.1:8b
    extra_hosts:
      - "host.docker.internal:host-gateway"
